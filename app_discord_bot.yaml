# For deployment using: `gcloud app deploy`
service: discord-bot
runtime: custom
env: flex

network:
  instance_ip_mode: internal

includes:
  - secrets.yaml

resources:
  cpu: 1
  memory_gb: 4
  disk_size_gb: 10
